TheFinal Design: Secure Idempotency Middleware1. The IdempotencyMiddleware:•Signature: <R> R process(PayeeDto request, Next<R> next)•Logic:◦Checks if the journey is configured for idempotency.◦Extracts the idempotency key from the PayeeDto.◦Calls the IdempotencyService to handle the core logic.2. The IdempotencyService:•This service will encapsulate all DB interactions.•It will use a TypeRegistry to safely map type aliases to classes.3. The IdempotencyRecord Entity:•Table: idempotency_store•Fields:◦idempotency_key (PK)◦status◦response_payload (Encrypted JSON)◦response_type_alias (e.g., "PAYEE_DTO")◦created_at4. The TypeRegistry:•A simple component that holds a Map<String, Class<?>>.•register("PAYEE_DTO", PayeeDto.class)•getClass("PAYEE_DTO") -> returns PayeeDto.class5. The PayloadEncryptionConverter:•An @AttributeConverter that encrypts/decrypts the response_payload field.•For this POC, it can be a simple Base64 encode/decode to simulate encryption without needing a key management setup.The Plan1.Create IdempotencyRecord Entity: With the new response_type_alias field.2.Create PayloadEncryptionConverter: The @AttributeConverter.3.Create TypeRegistry: The secure mapping component.4.Create IdempotencyService: The core logic service.5.Create IdempotencyMiddleware: The pipeline component.6.Update PayeeDto: Add the idempotencyKey field.7.Update PipelineOrchestrator: Add the new middleware to the pipeline.This is a production-grade, secure, and architecturally sound design.